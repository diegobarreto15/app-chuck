#Agregamos todos los archivos del proyecto al commit

git add .

#Hacemos el commit con un mensaje

git commit -m "Este es un commit automatico"

#Pusehamos el commit al repositorio remoto

git push origin master

#Mostrar estatus del repositorio local

git status
